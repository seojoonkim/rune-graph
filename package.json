{
  "name": "rune-hub",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @rune-hub/web",
    "build": "npm run build -w @rune-hub/web",
    "lint": "npm run lint -w @rune-hub/web"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
